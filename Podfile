# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'BDD-Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BDD-Weather
  
  target 'BDD-WeatherTests' do
      inherit! :search_paths
      pod 'Quick'
      pod 'Nimble'
  end

  target 'BDD-WeatherUITests' do
      inherit! :search_paths
      pod 'Quick'
      pod 'Nimble'
  end

end
